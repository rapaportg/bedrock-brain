apiVersion: v1
kind: Service
metadata:
  name: {{ include "brain-api.fullname" . }}
  labels:
    {{- include "brain-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "brain-api.selectorLabels" . | nindent 4 }}
