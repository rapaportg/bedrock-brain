version: "3.9"

# =============================================================================
# bedrock-brain — DigitalOcean production stack
#
# Differences from docker-compose.yml (local dev):
#   - MinIO and minio-init removed  → use DigitalOcean Spaces (S3-compatible)
#   - nginx added                   → SSL termination + reverse proxy
#   - Hot-reload volume mounts removed
#   - App images pulled from DOCR (set via REGISTRY + IMAGE_TAG env vars)
#
# Usage:
#   REGISTRY=registry.digitalocean.com/<name> IMAGE_TAG=<sha> \
#     docker compose -f docker-compose.yml -f infra/digitalocean/docker-compose.prod.yml up -d
#
# deploy.sh sets REGISTRY and IMAGE_TAG automatically.
# =============================================================================

networks:
  bedrock:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  keycloak_data:
  certbot_www:
  certbot_conf:

services:

  # ---------------------------------------------------------------------------
  # Postgres — same as dev; in future migrate to DO Managed Postgres
  # ---------------------------------------------------------------------------
  postgres:
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/migrations:/docker-entrypoint-initdb.d:ro

  # ---------------------------------------------------------------------------
  # PgBouncer — unchanged
  # ---------------------------------------------------------------------------
  pgbouncer:
    restart: always

  # ---------------------------------------------------------------------------
  # Redis — unchanged
  # ---------------------------------------------------------------------------
  redis:
    restart: always
    volumes:
      - redis_data:/data

  # ---------------------------------------------------------------------------
  # Keycloak — must know its public hostname for OIDC redirect URIs
  # ---------------------------------------------------------------------------
  keycloak:
    restart: always
    volumes:
      - keycloak_data:/opt/keycloak/data
      - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
    environment:
      KC_HOSTNAME: ${KEYCLOAK_PUBLIC_HOST}           # e.g. auth.yourdomain.com
      KC_PROXY: edge                                  # nginx terminates TLS
      KC_HOSTNAME_STRICT: "true"
      KC_HOSTNAME_STRICT_HTTPS: "true"
      KC_HTTP_ENABLED: "true"

  # ---------------------------------------------------------------------------
  # Brain API — pre-built image from DOCR, no hot-reload mount
  # ---------------------------------------------------------------------------
  brain-api:
    image: ${REGISTRY}/brain-api:${IMAGE_TAG}
    restart: always
    volumes: []                                       # override dev bind mount
    environment:
      ENVIRONMENT: production

  # ---------------------------------------------------------------------------
  # MCP Gateway — pre-built image from DOCR, no hot-reload mount
  # ---------------------------------------------------------------------------
  mcp-gateway:
    image: ${REGISTRY}/mcp-gateway:${IMAGE_TAG}
    restart: always
    volumes: []

  # ---------------------------------------------------------------------------
  # Brain Web — pre-built image from DOCR, no hot-reload mount
  # VITE_* vars are baked in at CI build time; not needed at runtime.
  # ---------------------------------------------------------------------------
  brain-web:
    image: ${REGISTRY}/brain-web:${IMAGE_TAG}
    restart: always

  # ---------------------------------------------------------------------------
  # Sync Bridge — pre-built image from DOCR, no hot-reload mount
  # ---------------------------------------------------------------------------
  sync-bridge:
    image: ${REGISTRY}/sync-bridge:${IMAGE_TAG}
    restart: always
    volumes: []

  # ---------------------------------------------------------------------------
  # Nginx — SSL termination + reverse proxy
  # Certbot manages certs in /etc/letsencrypt (mounted via certbot_conf volume)
  # ---------------------------------------------------------------------------
  nginx:
    image: nginx:1.25-alpine
    restart: always
    networks: [bedrock]
    depends_on:
      - brain-web
      - brain-api
      - keycloak
      - mcp-gateway
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/digitalocean/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - certbot_www:/var/www/certbot:ro
      - certbot_conf:/etc/letsencrypt:ro

  # ---------------------------------------------------------------------------
  # Certbot — renews certs on a timer; run manually for first-time issuance
  # First-time: docker compose run --rm certbot certonly ...
  # ---------------------------------------------------------------------------
  certbot:
    image: certbot/certbot:latest
    networks: [bedrock]
    volumes:
      - certbot_www:/var/www/certbot
      - certbot_conf:/etc/letsencrypt
    entrypoint: >
      /bin/sh -c "
        trap exit TERM;
        while :; do
          certbot renew --webroot -w /var/www/certbot --quiet;
          sleep 12h & wait $${!};
        done
      "
